#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen - checks the length of a string
 * @s:input
 * Return: Always 0 (Success)
 * Author: Bigavrii
*/

int _strlen(char *s)

{
	int lenght = strlen(s);

	return (lenght);

}
