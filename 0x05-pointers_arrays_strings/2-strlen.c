#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * main - Entry point
 *
 * @program to return string
 *
 * Return: 0 Always
 */

int _strlen(char *s)

{
	int lenght = strlen(s);

	return (lenght);

}
