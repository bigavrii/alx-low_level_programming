#ifndef FUNCTION_LIKE_MACRO_H
#define FUNCTION_LIKE_MACRO_H

/*
 * File: 3-function_like_macro.h
 * Desc: A file that writes a function-like macro ABS(x)
 *  computes the absolute value of a number x.
 */

#define ABS(x) ((x) < (0) ? -(x) : (x))

#endif
